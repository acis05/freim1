import { db } from "@/lib/db";import { compare } from "bcryptjs";import { createPlatformSession } from "@/lib/platform-auth";import { redirect } from "next/navigation";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;async function login(fd:FormData){"use server";const email=String(fd.get("email")||"").toLowerCase();const pass=String(fd.get("password")||"");const a=await db.platformAdmin.findUnique({where:{email}});if(!a||!a.active||!await compare(pass,a.passwordHash))redirect("/platform/login?error=1");await createPlatformSession(a.id);redirect("/platform/tenants")}return <div className="login-wrap"><div className="auth-card"><div className="top-brand"><img className="brand-icon" src="/brand/freim-icon.png"/><div><strong>Freim Apps Platform</strong><div className="muted">SaaS administration</div></div></div>{q.error&&<div className="alert">Email atau password platform salah.</div>}<form action={login} style={{marginTop:20}}><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required/></div><button className="btn" style={{width:"100%"}}>Platform Login</button></form></div></div>}
