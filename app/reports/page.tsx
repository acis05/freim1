import AppShell from "@/components/AppShell";import { requireRoles } from "@/lib/auth";import { Role } from "@prisma/client";export default async function Page(){await requireRoles([Role.FINANCE,Role.ADMIN,Role.MANAGEMENT]);return <AppShell title="Reports & Export"><div className="grid two-col"><div className="card"><h2>Job Costing & Margin</h2><p>Revenue, estimate, actual vendor bills, reimbursement, cash advance, variance, GP dan margin.</p><a className="btn" href="/api/reports/job-costing">Download Excel</a></div><div className="card"><h2>Reimbursements</h2><p>Claim, employee, job, customer, total, status, duplicate flag.</p><a className="btn" href="/api/reports/reimbursements">Download Excel</a></div></div></AppShell>}
