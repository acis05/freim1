"use client";import { useState } from "react";
type Job={id:string;jobNo:string;customer:{name:string}};type Cat={id:string;name:string};type Vendor={id:string;code:string;name:string};
export default function ReimbursementForm({jobs,cats,vendors,action}:{jobs:Job[];cats:Cat[];vendors:Vendor[];action:(fd:FormData)=>void|Promise<void>}){const [rows,setRows]=useState([0]);return <form action={action} encType="multipart/form-data"><div className="field"><label>Job Number</label><select name="jobId"><option value="">Non-job / General</option>{jobs.map(j=><option key={j.id} value={j.id}>{j.jobNo} · {j.customer.name}</option>)}</select></div><div className="items-head"><b>Expense Items</b><button className="btn secondary" type="button" onClick={()=>setRows(r=>[...r,Date.now()+Math.random()])}>+ Add Item</button></div>{rows.map((key,i)=><div className="expense-item" key={key}><div className="items-head"><b>Item {i+1}</b>{rows.length>1&&<button type="button" className="link-danger" onClick={()=>setRows(r=>r.filter((_,x)=>x!==i))}>Remove</button>}</div><div className="row"><div className="field"><label>Expense Date</label><input type="date" name="expenseDate" required/></div><div className="field"><label>Category</label><select name="categoryId" required>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div></div><div className="row"><div className="field"><label>Vendor Master</label><select name="vendorId"><option value="">Manual / none</option>{vendors.map(v=><option key={v.id} value={v.id}>{v.code} - {v.name}</option>)}</select></div><div className="field"><label>Vendor Name (optional)</label><input name="vendor" placeholder="Jika tidak ada di master"/></div></div><div className="field"><label>Amount (IDR)</label><input name="amount" type="number" min="1" required/></div><div className="field"><label>Description</label><input name="description" required/></div><div className="field"><label>Receipt / Invoice</label><input name="receipt" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" capture="environment"/><small className="muted">Max 10 MB. Duplicate check hanya dibandingkan dengan receipt tenant Anda sendiri.</small></div></div>)}<div className="field"><label>Notes</label><textarea name="notes" rows={2}/></div><button className="btn">Submit Reimbursement</button></form>}
